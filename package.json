{
  "name": "erhan-x-oto-haber",
  "version": "1.0.0",
  "private": true,
  "description": "Haberleri tarayan, kısa ve tarafsız özetleyerek X hesabında paylaşan otomasyon.",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/news.js && node --check src/summarizer.js && node --check src/state.js && node --check src/x-client.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@xdevplatform/xdk": "^0.6.6",
    "cheerio": "^1.1.2"
  }
}
